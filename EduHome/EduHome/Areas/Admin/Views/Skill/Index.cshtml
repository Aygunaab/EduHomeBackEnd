@model List<Skill>
@{
    ViewData["Title"] = "Index";
}
<!-- Button trigger modal -->
<!-- Modal -->


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 stretch-card">
                <div class="card">
                    <div class="card-body">

                        <a asp-controller="skill" asp-action="create" class="btn btn-success mb-5 ">Create new  Skill</a>
                        <h4 class="card-title">Social List</h4>

                        <div class="table-responsive pt-3">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>
                                            id
                                        </th>
                                        <th>
                                            Name
                                        </th>
                                       
                                        <th>
                                            Actions
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var skill in Model)
                                    {
                                        <tr class="table-success">
                                            <td>
                                                @skill.Id
                                            </td>
                                            <td>
                                                @skill.Name
                                            </td>
                                            
                                            <td>
                                                <a class="btn btn-primary" asp-controller="Skill" asp-action="Detail" asp-route-id="@skill.Id">Detail</a>
                                                <a class="btn btn-warning" asp-controller="Skill" asp-action="Update" asp-route-id="@skill.Id">Update</a>
                                                <a class="btn btn-warning" asp-controller="Skill" asp-action="Delete" asp-route-id="@skill.Id">Delete</a>




                                            </td>

                                        </tr>

                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

