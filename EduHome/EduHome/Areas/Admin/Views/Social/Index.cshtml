@model List<Social>
@{
    ViewData["Title"] = "Index";
}
<!-- Button trigger modal -->
<!-- Modal -->


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 stretch-card">
                <div class="card">
                    <div class="card-body">

                        <a asp-controller="social" asp-action="create" class="btn btn-success mb-5 ">Create new  Social</a>
                        <h4 class="card-title">Social List</h4>

                        <div class="table-responsive pt-3">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>
                                            id
                                        </th>
                                        <th>
                                            Link
                                        </th>
                                        <th>
                                            Icon
                                        </th>
                                        <th>
                                            Actions
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var social in Model)
                                    {
                                        <tr class="table-success">
                                            <td>
                                                @social.Id
                                            </td>
                                            <td>
                                                @social.Link
                                            </td>
                                            <td>
                                                @social.Icon
                                            </td>
                                            <td>
                                                <a class="btn btn-primary" asp-controller="Social" asp-action="Detail" asp-route-id="@social.Id">Detail</a>
                                                <a class="btn btn-warning" asp-controller="Social" asp-action="Update" asp-route-id="@social.Id">Update</a>
                                                <a class="btn btn-danger" asp-controller="Social" asp-action="Delete" asp-route-id="@social.Id">Delete</a>




                                            </td>

                                        </tr>

                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

