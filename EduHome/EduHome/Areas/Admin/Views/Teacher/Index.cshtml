

@model List<Teacher>
@{
    ViewData["Title"] = "Index";
}

<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Databasede olan Teacher modeller</h4>

                        <div class="table-responsive pt-3">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>
                                            #
                                        </th>
                                        <th>
                                            Image
                                        </th>
                                        <th>
                                            Title
                                        </th>
                                        <th>
                                            Description
                                        </th>
                                        <th>
                                            Action
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>

                                    @foreach (var teacher in Model)
                                    {
                                        <tr class="table-info">
                                            <td>
                                                @teacher.Id
                                            </td>
                                            <td>
                                                <img src="~/img/slider/@teacher.Image" style="width:100px;height:100px;" />
                                            </td>
                                            <td>
                                                @teacher.Name
                                            </td>
                                            <td>
                                                @Html.Raw(teacher.Description)
                                            </td>
                                            <td>
                                                <a class="btn btn-primary mb-2" style=" width:150px;" asp-controller="Teacher" asp-action="Index">Detail</a>
                                                <a class="btn btn-secondary mb-2" style=" width:150px;" asp-controller="Teacher" asp-action="update" asp-route-id="@teacher.Id">Update</a>
                                                <a class="btn btn-danger" style=" width:150px; " asp-controller="Teacher" asp-action="delete" asp-route-id="@teacher.Id">Delete</a>
                                            </td>

                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

